{
  "pluginAlias": "HomGar",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for HomGar, Diivoo, and RainPoint smart garden irrigation systems.",
  "footerDisplay": "Create a dedicated account in the HomGar/Diivoo app and share your home with it. Using your main account will cause the app to log out when the plugin authenticates.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "HomGar",
        "required": true
      },
      "email": {
        "title": "Email",
        "type": "string",
        "required": true,
        "description": "Your HomGar/Diivoo account email (use a dedicated account, not your main one)"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "Your HomGar/Diivoo account password"
      },
      "areaCode": {
        "title": "Country Code",
        "type": "string",
        "default": "44",
        "description": "Phone country code for your account (44 for UK, 1 for US)"
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 120,
        "minimum": 60,
        "description": "How often to check device status (minimum 60 seconds)"
      },
      "defaultWateringDuration": {
        "title": "Default Watering Duration (seconds)",
        "type": "integer",
        "default": 600,
        "minimum": 60,
        "maximum": 3600,
        "description": "Default duration when starting watering from HomeKit (10 minutes default)"
      },
      "weather": {
        "title": "Smart Irrigation",
        "type": "object",
        "expandable": true,
        "description": "Weather-aware 'Should Water' sensor using Open-Meteo (free, no API key). Tracks water balance using rainfall and evapotranspiration. Use in HomeKit automations to control your valves.",
        "properties": {
          "enabled": {
            "title": "Enable Smart Irrigation",
            "type": "boolean",
            "default": false
          },
          "latitude": {
            "title": "Latitude",
            "type": "number",
            "description": "Your location latitude for weather data"
          },
          "longitude": {
            "title": "Longitude",
            "type": "number",
            "description": "Your location longitude for weather data"
          },
          "pollInterval": {
            "title": "Weather Poll Interval (minutes)",
            "type": "integer",
            "default": 30,
            "minimum": 10,
            "description": "How often to check weather and update the Should Water sensor"
          },
          "deficitThreshold": {
            "title": "Water Deficit Threshold (mm)",
            "type": "number",
            "default": -2,
            "maximum": 0,
            "description": "Should Water activates when soil water balance drops below this value. More negative = drier before watering triggers."
          },
          "forecastSkipThreshold": {
            "title": "Forecast Skip Threshold (mm)",
            "type": "number",
            "default": 8,
            "description": "Skip watering if this much rain is forecast in the next 24 hours"
          },
          "windSkipThreshold": {
            "title": "Wind Skip Threshold (km/h)",
            "type": "number",
            "default": 30,
            "description": "Skip watering if wind speed exceeds this value (drift and evaporation)"
          },
          "seasonalAdjust": {
            "title": "Seasonal Adjustment",
            "type": "boolean",
            "default": true,
            "description": "Automatically skip watering in winter (Dec-Feb) and protect against frost (<2C)"
          }
        }
      }
    }
  }
}
