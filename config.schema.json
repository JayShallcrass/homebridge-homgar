{
  "pluginAlias": "HomGar",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for HomGar, Diivoo, and RainPoint smart garden irrigation systems.",
  "footerDisplay": "Create a dedicated account in the HomGar/Diivoo app and share your home with it. Using your main account will cause the app to log out when the plugin authenticates.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "HomGar",
        "required": true
      },
      "email": {
        "title": "Email",
        "type": "string",
        "required": true,
        "description": "Your HomGar/Diivoo account email (use a dedicated account, not your main one)"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "Your HomGar/Diivoo account password"
      },
      "areaCode": {
        "title": "Country Code",
        "type": "string",
        "default": "44",
        "description": "Phone country code for your account (44 for UK, 1 for US)"
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 120,
        "minimum": 60,
        "description": "How often to check device status (minimum 60 seconds)"
      },
      "defaultWateringDuration": {
        "title": "Default Watering Duration (seconds)",
        "type": "integer",
        "default": 600,
        "minimum": 60,
        "maximum": 3600,
        "description": "Default duration when starting watering from HomeKit (10 minutes default)"
      },
      "weather": {
        "title": "Weather Sensors",
        "type": "object",
        "expandable": true,
        "description": "Expose weather data as HomeKit sensors using Open-Meteo (free, no API key needed). Use the 'Should Water' sensor to trigger automations.",
        "properties": {
          "enabled": {
            "title": "Enable Weather Sensors",
            "type": "boolean",
            "default": false
          },
          "latitude": {
            "title": "Latitude",
            "type": "number",
            "description": "Your location latitude for weather data"
          },
          "longitude": {
            "title": "Longitude",
            "type": "number",
            "description": "Your location longitude for weather data"
          },
          "pollInterval": {
            "title": "Weather Poll Interval (minutes)",
            "type": "integer",
            "default": 30,
            "minimum": 10,
            "description": "How often to fetch weather data"
          },
          "rainSkipThreshold": {
            "title": "Rain Skip Threshold (mm)",
            "type": "number",
            "default": 10,
            "description": "'Should Water' turns off if this much rain fell in the last 48 hours"
          },
          "forecastSkipThreshold": {
            "title": "Forecast Skip Threshold (mm)",
            "type": "number",
            "default": 5,
            "description": "'Should Water' turns off if this much rain is forecast in the next 24 hours"
          },
          "seasonalAdjust": {
            "title": "Seasonal Adjustment",
            "type": "boolean",
            "default": true,
            "description": "'Should Water' turns off automatically in winter (Dec-Feb)"
          }
        }
      }
    }
  }
}
